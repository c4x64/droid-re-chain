{
  "mcpServers": {
    "droid-re-chain": {
      "command": "bash",
      "args": ["${PROJECT_ROOT}/mcp-entrypoint.sh"],
      "env": {
        "ANDROID_NDK_HOME": "${ANDROID_NDK_HOME:-}"
      }
    }
  }
}